# Ruff linter configuration
# See: https://docs.astral.sh/ruff/

line-length = 120

[lint]
# E402: module-import-not-at-top — intentional in main.py (load_dotenv before imports)
# E722: bare-except — allow in existing code for now (tracked for cleanup)
ignore = ["E402", "E701", "E722"]
